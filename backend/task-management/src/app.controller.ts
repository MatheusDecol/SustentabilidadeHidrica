import { Controller } from '@nestjs/common';
import { AppService } from './app.service';

@Controller()
export class AppController {
  // Se não há rotas/pontos finais, pode deixar vazio ou adicionar endpoints conforme necessário
}